<%= simple_form_for(human) do |f| %>
  <%= f.input :name %>
  <%= f.input :description %>
  <%= f.input :address %>
  <%= f.input :rate, label: 'Price', error: 'Price should in 1 to 10'%>
  <%= f.input :photo, as: :file %>
  <%= f.submit %>
<% end %>